dist: trusty
sudo: required

language: c

os:
  - linux

compiler:
  - gcc

addons:
  apt:
    update: true
    packages:
      - valgrind
      - cppcheck

script:
  - mkdir build
  - cd build
  - cmake ..
  - valgrind --tool=memcheck --leak-check=summary ./tests









  cppcheck -q --enable=all --inconclusive ./srcсзз